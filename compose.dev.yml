services:
  app:
    image: node:22-alpine
    working_dir: /app

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0"

volumes:
  node_modules:
